import config from "./config-styles";

export default {
  root: {
    color: config.colors.white,
    width: "100%",
    height: "5vh",
    background: `linear-gradient(${config.colors.blueGradientLight}, ${config.colors.blueGradientDark})`
  }
}